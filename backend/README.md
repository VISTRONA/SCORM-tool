# Backend / Logical Service Boundary

This folder represents the logical service/API layer only. A separate backend is not mandated by the blueprint.

The team may implement logical contracts using:

- a traditional backend
- local desktop service
- browser-side processing
- hybrid architecture

The required behavior is documented in `docs/API_CONTRACTS.md`.
